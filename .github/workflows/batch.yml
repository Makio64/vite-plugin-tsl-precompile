name: Batch harness (Phase 6)

# Runs the full 206-example harness. Target: >=120/199 passing.
# Skipped on every push because it's expensive; runs nightly and on demand.

on:
  schedule:
    - cron: '17 5 * * *'   # 05:17 UTC daily
  workflow_dispatch:

jobs:
  batch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: npx playwright install chromium
      # Needs a local three.js examples checkout (`--three-repo`); install is
      # green without sibling deps, but the harness itself is still opt-in until
      # CI vendors/clones three.js examples and a pass threshold is locked.
      - run: pnpm test:batch
        continue-on-error: true  # TODO: enforce threshold once Phase 6 lands
