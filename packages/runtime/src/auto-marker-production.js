// Automatic marker calls are rewritten during production builds. This
// conditional-export target intentionally has no side effects or runtime
// imports, allowing the injected development bootstrap import to disappear.
export {};
