/**
 * Build-injected full-Three compatibility path. It validates and registers
 * the captured artifact while preserving the exact live material identity.
 */
export function __applyPrecompiled<TMaterial>( material: TMaterial, artifactModule: unknown, expectedHash: string ): TMaterial;
