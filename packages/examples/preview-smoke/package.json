{
  "name": "examples-preview-smoke",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Playwright smoke that builds an example, spawns `vite preview`, and asserts the production canvas renders + animates. Locks in the ocean-preview-pipeline regression.",
  "scripts": {
    "test": "node run.mjs --example=ocean",
    "test:preview": "node run.mjs --example=ocean"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}
